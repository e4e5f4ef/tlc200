# Бриф Toyota Land Cruiser 200 — нотатки проєкту

Карта проєкту та ключові рішення. Оновлювати при суттєвих змінах.
Мета файлу: щоб у новій сесії (або інший розробник) за хвилину підхопив контекст.

## Що це
Веб-додаток для менеджера: заповнює комплектацію тюнінгу LC200 → генерує PDF-специфікацію.
Один самодостатній файл `index.html` (працює офлайн). Усі фото вбудовані як base64 (JPEG),
логотип — SVG, темний фон/плашки в PDF — 3 крихітні PNG-пікселі (data-URI).

## Деплой
- Репозиторій: github.com/e4e5f4ef/tlc200
- GitHub Pages: https://e4e5f4ef.github.io/tlc200/
- Робоча тека на Mac: `~/Documents/landcruiser-brief/`
- Коміт: подвійний клік `commit.command` (git add index.html → commit → push).
  УВАГА: скрипт додає лише `index.html`. Щоб закомітити й цей NOTES.md — раз виконати
  `git add NOTES.md` або замінити в скрипті рядок на `git add -A`.

## Тема / кольори
Червоно-чорна. ОБРАНЕ = зелене (--green #1f9d4d / --green-2 #2fd06a).
Червоний (--red #d3132b / --red-2 #ff2740) = бренд (лого, заголовки) + помилки/валідація.
Для PDF: константи RED/GREEN/DARK/PANEL — це data-URI пікселі (фон/плашки друкуються як
зображення, тож не залежать від галочки «фонова графіка»).

## Структура форми
Шапка: № замовлення, ПІБ, телефон (фікс. +380 + 9 цифр, автоформат), рік випуску
(2007–2015 / 2015–2021), паливо (Бензин/Дизель). Дві вкладки: Екстер'єр / Інтер'єр.

Екстер'єр — 3 під-секції (.subsec):
1. Верхній блок (залежить від року):
   - 2007–2015 → «Базовий комплект оновлення» (фото До/Після + Склад комплекту).
   - 2015–2021 → «Land Cruiser 200 2015–2021. Базова комплектація» (фото перед/зад).
2. «Вибір варіантів комплектуючих»: optics, grille, taillight, reflector, skirts(Обвіс), intercooler.
3. «Додаткові опції» (так/ні): mirror, sidestep, molding, wheels, spoiler, fender, handle.

Інтер'єр: cabin, wheel, gear, sills, keycase.

## Конфіг (у `<script>`)
- `SINGLE_EXT`, `SINGLE_INT` — групи одиничного вибору: {key, label, options[]}.
- `EXTRAS` — так/ні опції: {key, label}. Назви (крім fender) у показі отримують
  суфікс «(стиль 2015–2021)» через `extraLabel(e)`.
- `PHOTOS` — об'єкт фото, ключ `"groupKey::Назва опції"` → data-URI JPEG.
  Базовий комплект: `basekit::ba`, `basekit::kit`. Базова комплектація: `basestock::front`, `basestock::back`.
- `CODES` — артикули (поки лише optics) → у PDF підпис «арт. …».
- `INCLUDED` — який варіант входить у базовий комплект (для 2007–2015):
  optics:Стандарт, grille:2016, taillight:Стандарт, reflector:Стандарт, intercooler:Бензин.
- `NONEED_STOCK` — групи, де варіант «Не потрібно» показується ЛИШЕ для 2015–2021:
  optics, grille, taillight, reflector.

## Ключова логіка
- `toggleBaseKit()` (на зміну року): показує basekit (2007–2015) або basestock (2015–2021);
  ставить клас `kit-mode`/`stock-mode` на #pane-ext; міняє підказку «Вибір варіантів…»;
  якщо вийшли зі stock — повертає «Не потрібно» назад на INCLUDED-варіант.
- `syncIntercooler()` (на зміну палива): Бензин→Бензин, Дизель→Дизель. Можна перебити вручну.
- Позначка «входить у базовий комплект» з'являється лише в `kit-mode` на INCLUDED-варіанті.
- Валідація в `generate()`: усі поля + телефон (regex 380 + 9 цифр) + КОЖНА група вибору
  має селект + КОЖНА так/ні опція обрана (стартують без вибору). Незаповнене підсвічується
  червоним, авто-скрол до першого, перемикання вкладки.

## PDF (функція generate)
- Сторінка = блок (page-break-before:always — надійно в Chrome).
- Порядок: обкладинка → (базовий блок за роком) → блоки Екстер'єру → Інтер'єр.
  sills+keycase ОБ'ЄДНАНІ на одну сторінку; підпис (Менеджер/Клієнт) пінниться внизу
  останньої сторінки (footHTML на останньому блоці).
- Нумерація: з 2-ї сторінки «N / усього» (у потоці, не накладається).
- `gl-keep` тримає заголовок блоку з першим рядом карток; картки — ряди по 2 (`ps-row2`),
  атомарні (не розриваються між сторінками).
- Невибрані картки приглушені (opacity .6), обрана — зелена рамка + ✓.
- Базовий комплект/комплектація: 2 фото стеком, висота обмежена (max-height ~78–95мм), щоб
  обидва влізли на сторінку. «Склад комплекту» — object-fit:contain (видно повністю).

## Прев'ю
- Кнопка «Згенерувати PDF» → модалка #preview зі сторінками; `fitPreview()` масштабує під
  ширину екрана (мобільні). Кнопки: Закрити / Завантажити-Друк (window.print()).

## Як міняти фото
Фото закодовані в тексті (base64), вручну не редагуються. Зміну робить Claude:
пере-кодовує нове фото і замінює значення відповідного ключа в `PHOTOS`.

## ПЛАН: ціни через API (ще не впроваджено)
Ідея: кожній опції присвоїти `sku` сайту, тягнути ціни, показувати доплату
(різниця від стандартного варіанта) + сумарну доплату — у формі і в PDF.
- API: https://ddaudio.com.ua/api/price/retail?lang=ua  (Bearer-токен, серверний).
- 100k+ товарів, до 10000 за запит, пагінація `offset`, поле `totalResults`,
  ліміт «не частіше разу на 5 сек». Стабільний ключ — `sku`; ціна — `price`/`sale_price` (UAH).
- БЕЗПЕКА: токен НЕ класти в `index.html` (файл публічний). Засвічений токен — перевипустити.
- Рекомендований шлях: периодичний знімок → маленький `prices.json` (за sku) поряд з index.html,
  бриф читає його (без токена, без CORS). Доплата = price(обране) − price(базового);
  «Стандарт/входить у комплект»/«Не потрібно» = 0.
- Невирішено: чи є endpoint «товар за sku» (щоб не тягнути весь прайс); опції = поодинокі
  товари чи набори кількох sku (тоді ціну сумувати); показувати sale_price чи звичайну.

## Файли
- `index.html` — додаток (єдиний робочий файл).
- `commit.command` — пуш на GitHub.
- `NOTES.md` — цей файл.
- (в архіві колись був brief-landcruiser200-BACKUP.html — стара версія, не використовується.)
